import React from 'react';

import './NoPage.scss';

function NoPage() {
  return (
    <>
        User will be redirected here if the called resource is not present in the server, e.g. incorrect url
    </>
  )
}

export default NoPage